<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokéCollect</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>

    <%- include('navbar') %>
    <header class="detail-header">
      <h1>Card Details</h1>  
    </header> 

    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <div class="card-image">
                    <img src="<%= card.url_image %>" alt="<%= card.card_name %>">
                </div>
            </div>
            <div class="col-md-6">
                <div class="card-info">
                    <h2><em><%= card.card_name %></em></h2> <!-- <em> is for italics, <strong> is for bold-->
                        
                    <!-- no need for if statement as all cards have these attributes-->
                    <p><strong>Value - </strong>$<%= card.value_price %></p>
                    <p><strong>Card Number - </strong><%= card.card_number %></p>
                    <p><strong>Illustrator - </strong><%= card.illustrator %></p>
                    <p><strong>Rarity - </strong><%= card.rarity_type %></p>
                    <p><strong>Expansion - </strong><%= card.expansion_name %></p>
                    <p><strong>Date of Release - </strong><%= card.release_date.toString().substring(3, 15) %></p>
                    <p><strong>Format - </strong><%= card.format %></p>
                    <p><strong>Card Type - </strong><%= card.type_name %></p>

                    <!--if statement to display or hide on webpage whether the card has these attributes or not-->
                    <% if (card.regulation_mark) { %>
                        <p><strong>Regulation Mark - </strong><%= card.regulation_mark %></p>
                    <% } %>
                    
                    <% if (card.HP) { %>
                        <p><strong>Pokémon HP - </strong><%= card.HP %></p>
                    <% } %>
                    
                    <% if (card.ability_description) { %>
                        <p><strong>Ability Description - </strong><%= card.ability_description %></p>
                    <% } %>
                    
                    <% if (card.attack_description) { %>
                        <p><strong>Attack Description -  </strong><%= card.attack_description %></p>
                    <% } %>

                    <% if (card.weakness) { %>
                        <p><strong>Weakness - </strong><%= card.weakness %></p>
                    <% } %>
                    
                    <% if (card.resistance) { %>
                        <p><strong>Resistance - </strong><%= card.resistance %></p>
                    <% } %>
                    
                    <% if (card.stage) { %>
                        <p><strong>Stage - </strong><%= card.stage %></p>
                    <% } %>
                    
                    <% if (card.energy_type) { %>
                        <p><strong>Energy Type - </strong><%= card.energy_type %></p>
                    <% } %>

                </div>
            <div class="collection-wishlist">
                <form action="/choose-collection/<%= card.card_id %>" method="GET">
                    <button type="submit" class="btn btn-dark btn-sm">Collection</button>
                </form>
                <button type="button" class="btn btn-danger btn-sm wishlist-button"> Wishlist </button>
            </div>
            <div class="back">
                <a href="/cards" class="btn btn-lg btn-dark">Go Back</a>
            </div>
        </div>
    </div>
</div>
    <%- include('footer') %>
    
</body>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>